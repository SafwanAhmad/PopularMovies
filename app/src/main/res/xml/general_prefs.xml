<?xml version="1.0" encoding="utf-8"?>

<!--Resource file for the preferences used in Settings-->
<!--Although you can instantiate new Preference objects at runtime,
    you should define your list of settings in XML with a hierarchy
    of Preference objects. Using an XML file to define your collection
    of settings is preferred because the file provides an easy-to-read
    structure that's simple to update.
    Also, your app's settings are generally pre-determined, although
    you can still modify the collection at runtime.-->

<PreferenceScreen
    xmlns:android="http://schemas.android.com/apk/res/android">

    <ListPreference
        android:title="@string/pref_sorting_order"
        android:key="@string/pref_sorting_order_key"
        android:defaultValue="@string/pref_sorting_popular"
        android:entryValues="@array/pref_sorting_values"
        android:entries="@array/pref_sorting_options"/>

</PreferenceScreen>